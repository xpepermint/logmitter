import { Spec } from '@hayspec/spec';
// import * as exp from '..';

const spec = new Spec();

// spec.test('exposed content', (ctx) => {
//   ctx.true(!!exp.Runner);
//   ctx.true(!!exp.Command);
// });

export default spec;
